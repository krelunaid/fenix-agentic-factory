# {{projectName}}

Generated from the FENIX web TypeScript scaffold. The template includes a React UI, Cloudflare Worker, D1 schema, health contract and fail-closed quality commands. Provisioning replaces template tokens, installs a minimal deterministic preview toolchain and pins dependency versions before the first build.
